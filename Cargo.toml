[package]
name = "hutch"
version = "0.1.0"
edition = "2024"
description = "Glenda Hosted Runtime"

[dependencies]
libc = "0.2"
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
libglenda-rs = { path = "../../lib/libglenda-rs", features = ["rt-none"] }
toml = "0.8"
rio = "0.9"
